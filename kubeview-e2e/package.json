{
  "name": "kubeview-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end tests: real backend + real frontend + real (kind) cluster, driven with Playwright.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  }
}
